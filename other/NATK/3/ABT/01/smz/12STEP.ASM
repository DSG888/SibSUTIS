; ================================
; ===== 12Step.asm ===============
; ===== Stepper Motor ============
	mov	al,1	out	05
	mov	al,2	out	05
	mov	al,4	out	05
	mov	al,8	out	05
	mov	al,9	out	05
	mov	al,1	out	05
	mov	al,3	out	05
	mov	al,2	out	05
	mov	al,6	out	05
	mov	al,4	out	05
	mov	al,c	out	05
	mov	al,8	out	05
	mov	al,9	out	05
	mov	al,1	out	05
	end
; ================================

	Задание 15
	==========
	Аналогично программе 08TABLE.ASM оформите значения, 
	последовательно передаваемые в регистр AL, в виде
	таблицы. Затем реализуйте цикл, в котором из созданной 
	таблицы будут последовательно выбираться эти значения
	для управления шаговым двигателем. Данные выбираются
	из начала таблицы по возрастанию адресов(вращение по
	часовой стрелке). Цикл заканчивается при достижении
	конца таблицы. Перед началом цикла в регистр CL заносится
	количество строк таблицы, DL=0, а в BL заносится начальный
	адрес таблицы. В самом цикле в регистр AL заносится
	очередной элемент из таблицы, происходит обращение к
	порту 05 и увеличиваются значения на 1 в регистрах BL и
	DL. Затем выполняется проверка на окончание цикла. 

	Задание 16
	==========
	Аналогично программе 08TABLE.ASM оформите значения, 
	последовательно передаваемые в регистр AL, в виде
	таблицы. Затем реализуйте цикл, в котором из созданной 
	таблицы будут последовательно выбираться эти значения
	для управления шаговым двигателем. Данные выбираются
	из начала таблицы по возрастанию адресов(вращение по
	часовой стрелке). Цикл заканчивается при достижении
	конца таблицы. Перед началом цикла в регистр CL заносится
	количество строк таблицы, а в BL заносится начальный
	адрес таблицы. В самом цикле в регистр AL заносится
	очередной элемент из таблицы, происходит обращение к
	порту 05, увеличивается значение на 1 в регистре BL и
	уменьшается на 1 значение в регистре CL. Затем выполняется 
	проверка на окончание цикла. 

	Задание 17
	==========
	Аналогично программе 08TABLE.ASM оформите значения, 
	последовательно передаваемые в регистр AL, в виде
	таблицы. Затем реализуйте цикл, в котором из созданной 
	таблицы будут последовательно выбираться эти значения
	для управления шаговым двигателем. Данные выбираются
	из конца таблицы по убыванию адресов(вращение против
	часовой стрелке). Цикл заканчивается при достижении
	начала таблицы. Перед началом цикла в регистр CL заносится
	количество строк таблицы, а в BL заносится конечный
	адрес таблицы. В самом цикле в регистр AL заносится
	очередной элемент из таблицы, происходит обращение к
	порту 05 и уменьшается значения на 1 в регистре BL.
	Затем выполняется проверка на окончание цикла. 