
	1 Вывести переменную var1 используя функцию 09h прерывания 21h.

	2 Вывести переменную var2 используя функцию 02h прерывания 21h.

	3 Для вывода переменных в отдельных строках необходимо перед '$'
в переменной var поместить символы: 0ah,0dh.

	4 Вывести переменную var2 используя функцию 02h прерывания 21h. Но
при этом необходимо организовать цикл с использованием команды loop и регистр 
cx: 
.data
message db 'Ассемблер - базовый язык компьютера'
len=$-message

.code
	mov	cx,len
	lea	si,message
	mov	ah,02h
.loop:
	mov	dl,[si]
	int	21h
	inc	si
	loop	.loop