#include <stdio.h>
#include <stdlib.h>
#include <conio.h>
#include <graphics.h>

#define N 4

float x[N + 1], y[N + 1];
float h[N];
float d[N], M[N];
float C[N][N];
float Cd[N][N + 1];

int main()
{
	
	return 0;
}
