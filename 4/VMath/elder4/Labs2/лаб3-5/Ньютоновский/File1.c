//---------------------------------------------------------------------------

#include <stdio.h>
#include <math.h>

//---------------------------------------------------------------------------

int main(int argc, char* argv[])
{
	float x,xc,c,cc=1.5,a,b,e=0;
	int i=1;
		printf("Uravnenie: x^2-3=0\n");
        xc=2;
        do
		{
				x=xc-(xc*xc-2)/(2*xc);
				printf("\n   SHAG[%d]: x%d=%f",i,i,x);
                e=x-xc;
                if(e<0)e*=-1;
				xc=x;
				++i;
		}while(!(fabs(e)<0.0000001));

	getchar();
	return 0;
}
//---------------------------------------------------------------------------
